# 问题解析：三重缺失与自封闭循环

> 以下是对该问题的一种可能的解析路径，不是唯一的解析。它试图解释为什么这个被关注了至少五年的问题至今没有被解决，以及为什么它在理论上是可解的。

## 真正缺失的不是一样东西，而是三样

表面上缺的是算法。但往下挖，会发现三个东西同时缺失：

1. **没有 ground truth** —— 不知道未知稀有亚群应该在哪里
2. **没有评估框架** —— 无法检测整合过程是否消灭了某个稀有亚群
3. **没有适用算法** —— 即使知道丢了，也没有工具能防止

算法缺失只是最后一环。

## 三重缺失为什么是连锁的

没有 ground truth，评估框架就建不起来——你不知道"保留住了"该怎么定义。

没有评估框架，就没有人知道现有算法在稀有亚群上失败了——失败信号不存在。

没有失败信号，就没有人去开发专门保护稀有亚群的算法——没有动力。

## 自封闭循环

三重缺失形成了一个封闭的循环：

```
没有 ground truth
→ 没有评估框架
→ 没有失败信号
→ 没有算法开发动力
→ 没有新发现
→ 没有 ground truth
```

这个循环的每一步都在等上一步先解决。没有任何一个环节能单独突破。

## 现有方法为什么没有触及这个问题

scDML、ResPAN 等方法声称能保留稀有亚群，但它们的验证依赖两种 ground truth：

- **模拟数据**：人工构造一个稀有群，整合后看它还在不在。这是循环论证——你知道它存在是因为你造的。
- **已有注释**：用数据集提供者的细胞注释作为标准答案。但如果一个亚群在注释里根本不存在，这些方法完全无法评估它是否被保留了。

这两种验证方式都回答不了核心问题：**对于一个事先未知的稀有亚群，整合有没有把它消灭？**

## 为什么至今没有人解决

不是因为问题不重要，也不是因为没有人注意到。

而是因为这个循环的入口被 ground truth 的缺失锁死了。所有人都默认 ground truth 必须从外部获取——要么是模拟数据，要么是实验验证。而外部 ground truth 的获取成本极高，且每次只能验证一个特定亚群，无法泛化。

这个默认假设本身就是问题的一部分。

## 为什么这个问题在理论上是可解的

循环虽然是封闭的，但它不是不可打破的。因为有一个关键事实：

**正常的批次校正和稀有亚群被消灭，在数据上的结构模式是不同的。**

正常批次校正：同一类型的细胞跨批次合并，群还在，只是成员来自更多批次。这是聚拢。

稀有亚群被消灭：一个群的成员被打散到多个不相关的大群里，群消失。这是弥散。

一个是聚拢，一个是弥散。这两种模式在结构上不同，不是噪声里的微弱差异。

这意味着：不需要外部 ground truth，也可以从整合前后的数据结构变化本身，区分出哪些变化是正常的，哪些是病理性的。信号存在于数据中，问题是至今没有人去设计提取它的框架。
